docker build -t ai-dubbing .

# 1-5
docker run -it --rm \
  -v "$(pwd)/main.py:/main.py" \
  -v "$(pwd)/source:/app/source" \
  -v "$(pwd)/.cache/whisper:/root/.cache/whisper" \
  -v "$(pwd)/.cache/huggingface:/root/.cache/huggingface" \
  ai-dubbing \
  python main.py




pip install openai-whisper transformers sentencepiece piper-tts dotenv torch torchaudio